const app = require('../scrapers/builtwith');

// test('builtWith returns a string', () => {
//   expect(app.sum(1, 2)).toBe(3);
// });


test('database has data, api not consulted', () => {
  
})

test('database missing data, api consulted', () => {
  
})

test('builtwith object has correct fields', () => {
  
})